# --- Postgres ---
POSTGRES_DB=SkinscoreInspectApiDatabase
POSTGRES_USER=skinscore
POSTGRES_PASSWORD=sk1nSC0R3!!        # if you keep special chars, see note below
DB_PORT=5440

# --- App / API ---
APP_PORT=2855
NODE_ENV=production

# If your DB password has special characters, prefer setting this fully to avoid URL-encoding issues:
# DATABASE_URL=postgres://skinscore:sk1nSC0R3!!@postgres:5440/SkinscoreInspectApiDatabase

# --- CORS (comma-separated) ---
ALLOWED_ORIGINS=https://skinscore.app,https://www.skinscore.app,https://inspect.skinscore.app,http://localhost:3000,http://localhost:3001,http://localhost:5173,http://localhost:8080,http://localhost:8081,https://api.skinscore.app
